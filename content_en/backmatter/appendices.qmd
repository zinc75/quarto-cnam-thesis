# Complete `_quarto.yml` configuration {.appendix #sec-yaml-config-file}

This appendix reproduces and comments on the `_quarto.yml` file in its entirety,
for quick reference.

## Document metadata for the cover page

```yaml
book:
  title: "Thesis title"
  subtitle: "Optional subtitle"        # remove if absent
  author: "First LAST"
  downloads: [pdf]                     # PDF button in the HTML sidebar

date-soutenance: "12 June 2026"        # DO NOT use date:
discipline: "60th CNU section — Mechanics, mechanical engineering, civil engineering"
specialite: "Acoustics"
ecole-doctorale: "Sciences des Métiers de l'Ingénieur"
laboratoire: "MACS Laboratory, Cnam Paris"
directeur: "Prof. First LAST, Full Professor, Cnam Paris"
cotutelle: false

dedicace: "Optional dedication…"       # remove if absent
```

## Jury composition for the cover page

```yaml
jury:
  - nom: "Ms First LAST"
    titre: "Title, Research Unit, Institution"
    role: "Chair"
  - nom: "Mr First LAST"
    titre: "Title, Research Unit, Institution"
    role: "Reviewer"
  - nom: "Ms First LAST"
    titre: "Title, Research Unit, Institution"
    role: "Reviewer"
  - nom: "Mr First LAST"
    titre: "Associate Professor HDR, Unit, Institution"
    role: "Examiner"
  - nom: "Mr First LAST"
    titre: "Full Professor, MACS, Cnam Paris"
    role: "Thesis supervisor"
```

## Infrastructure section options

```yaml
glossaire: true          # true = loads the LaTeX glossaries package
pagestyle-sections: true # true = roman/arabic/Roman numbering per section
bibliography-manual: true
minitoc-newpage: false   # true = page break after each mini-TOC
chapter-openright: true  # false = for digital version without right-hand page
index: false             # true = generates an index via \index{term}
```

## Collaborative comments

```yaml
comments:
  enabled: true          # false before final deposit on theses.fr
  show_author: true
  authors:
    author:
      name: "First LAST"
      color_html: "#005EA5"
      color_latex: "blue!20"
    supervisor:
      name: "Supervisor"
      color_html: "#D55E00"
      color_latex: "orange!30"
```

## Python execution

```yaml
execute:
  freeze: auto    # recomputes only if the chunk code changes
  echo: true      # shows code by default (overridable per chunk)
```

# PDF/A-1b submission on theses.fr {.appendix #sec-pdfa}

The French national thesis repository **theses.fr** requires a PDF/A-1b file
conforming to ISO 19005-1. The PDF produced by this template is **directly valid**
according to the reference validator — no post-processing is needed in the standard case.

## Reference validator

Use **[facile.cines.fr](https://facile.cines.fr)** (CINES, veraPDF engine) to validate
the PDF before deposit. This is the reference validator for French institutional
archiving platforms, including theses.fr.

::: {.callout-warning}
## Do not use pdfforge.org with this template

pdfforge.org produces **false positives** on PDFs generated by this template:
it flags §6.4 violations (callout transparency) and §6.3.5/§6.3.6 (font metrics)
that are not detected by veraPDF. Callout transparency is suppressed directly in the
LaTeX template; FontAwesome5 metrics are compliant according to veraPDF.
Do not use pdfforge to validate an institutional deposit.
:::

## When to use Ghostscript

Ghostscript post-processing is only needed if the thesis contains matplotlib figures
in **vector format** (`fig-format: pdf` in `_quarto.yml`) with transparency (`alpha < 1`),
for example:

```python
ax.grid(True, alpha=0.3)   # generates PDF transparency operators in vector mode
```

In the **standard workflow** (PNG figures, the default for Quarto+pdflatex),
no §6.4 violations are produced and Ghostscript is not needed.

## Installing Ghostscript

Ghostscript is **not included** in TeX Live — it must be installed separately.

::: {.callout-note}
## Installation by platform

**macOS:**
```bash
brew install ghostscript
```

**Linux (Debian/Ubuntu):**
```bash
apt install ghostscript
```

**Linux (Fedora/RHEL):**
```bash
dnf install ghostscript
```

**Windows:**
```powershell
winget install ArtifexSoftware.GhostScript
```
:::

## Submission workflow

The PDF/A-1b conversion is activated by adding `pdfa` as a third argument in
`_quarto-en.yml` (or `_quarto-fr.yml`):

```yaml
# In _quarto-en.yml — for final submission only:
project:
  post-render:
    - ./_scripts/postrender.sh en _thesis-en pdfa
    # Windows: ./_scripts/postrender.bat en _thesis-en pdfa
```

Then render the PDF:

```bash
quarto render --profile en --to cnam-thesis-pdf
```

The script prints sizes before and after:

```
Running Ghostscript PDF/A-1b conversion…
PDF/A-1b conversion done: 835 KB → 1403 KB
Validate on facile.cines.fr before submitting to theses.fr.
```

::: {.callout-warning}
## Mandatory validation before deposit

Validate the PDF on **[facile.cines.fr](https://facile.cines.fr)** before submitting
to theses.fr. Ghostscript corrects vector transparency violations but does not
guarantee 100% compliance in all edge cases.
:::

::: {.callout-tip}
## After submission

Restore the line without `pdfa` in `_quarto-en.yml` for day-to-day builds —
GS conversion adds approximately 1–2 minutes of processing and is only needed
for the final deposit.
:::

## File size increase

With vector matplotlib figures using transparency, conversion causes a size increase
of approximately **60–70%** (vector transparency flattening).
To avoid this, use opaque colours directly in matplotlib:

```python
# Instead of:   ax.grid(True, alpha=0.3)
ax.grid(True, color='lightgray')   # visual equivalent, no transparency
```
